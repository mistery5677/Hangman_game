#pragma once // This will just "compile" once, to not compile multiple times
#include <vector> // The '#' is a pre-processor that the compilation will have
#include <string>

std::vector<std::string> read_file();